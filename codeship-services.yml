server:
  build: server
  links:
    - mongo:mongodb.cloud66.local

test:
  build: test
  command: mocha
  encrypted_env_file: cloud66-deployment.env.encrypted
  links:
    - mongo:mongodb.cloud66.local
    - server

mongo:
  image: mongo